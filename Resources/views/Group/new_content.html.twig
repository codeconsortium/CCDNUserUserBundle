{% form_theme form theme %}
<form action="{{ path('fos_user_group_new') }}" {{ form_enctype(form) }} method="POST" class="form-horizontal">
    {{ form_widget(form) }}
    <div>
        <input type="submit" class="btn btn-primary" value="{{ 'group.new.submit'|trans([], 'FOSUserBundle') }}">
    </div>
</form>
